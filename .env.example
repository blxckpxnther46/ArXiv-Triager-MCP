# Notion API Integration Token (Internal integration token starting with secret_)
NOTION_TOKEN=your_notion_integration_token

# Server Port
PORT=3000

# Optional MCP Bearer Auth Token for transport security (leave blank to disable bearer check)
MCP_AUTH_TOKEN=change_this_to_a_secure_random_token
